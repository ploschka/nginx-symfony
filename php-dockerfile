FROM php:8.1-fpm

WORKDIR /app

RUN apt-get clean && apt-get update && apt-get install -y \
    curl \
    libxml2-dev \
    git
RUN apt-get clean && rm -rf /var/lib/apt/lists/*

RUN docker-php-ext-install pdo_mysql ctype xml dom xmlwriter session

RUN curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer

EXPOSE 9000
CMD /bin/sh -c "composer update && composer symfony:dump-env prod && php-fpm"
